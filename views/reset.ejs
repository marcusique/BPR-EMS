<!DOCTYPE html>
<html>
  <head>
    <title>Employee Management System</title>
    <!--Import Google Icon Font-->
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
    <!--Import materialize.css-->
    <link
      type="text/css"
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
      media="screen,projection"
    />
    <link rel="icon" href="/favicon.png" sizes="32x32" />
    <script
      type="text/javascript"
      src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"
    ></script>
    <!---Import custom css file-->
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <!--Let browser know website is optimized for mobile-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <div class="row">
      <div class="col l12 m12 s12 greeting">
        <h2>Password reset</h2>
        <h6>enter your new password</h6>
        <div class="loginForm col l4 m5 s5 offset-l4 offset-m4 offset-s3">
          <form action="/reset/<%= token %>" method="POST">
            <div class="input-field">
              <label for="password">New Password</label>
              <input type="password" name="password" value="" />
            </div>
            <div class="input-field">
              <label for="confirm">Confirm Password</label>
              <input type="password" name="confirm" value="" />
            </div>
            <div>
              <button type="submit" class="btn-small white black-text">
                Update Password
              </button>
              <a href="javascript:history.back()" class="btn-small white black-text">
                Cancel
              </a>
            </div>
          </form>
        </div>
      </div>
    </div>
    <!-- Handling toasts with success and error messages -->
    <% if (success && success.length > 0) { %>
    <script>
      M.toast({ html: '<%= success %>' });
    </script>
    <% } else if (error && error.length > 0) { %>
    <script>
      M.toast({ html: '<%= error %>' });
    </script>
    <% } %>
  </body>
</html>
